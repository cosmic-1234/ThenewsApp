<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thenewsapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic__65070_article_blog_content_news_note_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic__65070_article_blog_content_news_note_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Thenewsapp">
        <activity android:name=".NewsDeatilActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>